name: Notify Slack Channel on Pull Request

on:
  push:
    branches-ignore:
      - dev
  pull_request:
    types: [closed, opened, synchronize, reopened]
    branches:
      - dev
      - main
      - staging

jobs:
  notify-on-pr:
    name: Notify Slack on Pull Request
    if: >
      github.event_name == 'pull_request' &&
      github.event_name != 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Send Slack notification
        if: github.event.action != 'synchronize'
        uses: slackapi/slack-github-action/@v2.1.0
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            {
              "channel": "${{ vars.SLACK_CHANNEL_ID }}",
              "text": "A new PR was just opened #${{ github.event.pull_request.number }} :rocket:",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "${{ github.event.pull_request.user.login }} opened a Pull Request :rocket:. \
                    \n\nTODO: update pr message"
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*Pull Request #${{ github.event.pull_request.number }}:*\
                      \n<${{ github.event.pull_request.html_url }}|${{ github.event.pull_request.title }}>"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Incoming Branch:*\n`${{ github.head_ref }}`"
                    }
                  ]
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": {
                        "type": "plain_text",
                        "text": "Preview"
                      },
                      "url": "https://nupat.africa"
                    }
                  ]
                }
              ]
            }
      - name: Slack notification on closed pull request
        if: github.event.action == 'close pull_request' 
        uses: slackapi/slack-github-action/@v2.1.0
        with:
            method: chat.postMessage
            token: ${{ secrets.SLACK_BOT_TOKEN }}
            payload: |
              {
              "channel": "${{ vars.SLACK_CHANNEL_ID }}",
              "text": "A Pull Request has been closed #${{ github.event.pull_request.number }} :rocket:",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "${{ github.event.pull_request.user.login }} closed a Pull Request :rocket:. \
                    \n\nTODO: update pr message"
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*Closed Pull Request with no URL #${{ github.event.pull_request.number }}:*\
                      \n<${{ github.event.pull_request.html_url }}|${{ github.event.pull_request.title }}>"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Incoming Branch:*\n`${{ github.head_ref }}`"
                    }
                  ]
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": {
                        "type": "plain_text",
                        "text": "Preview"
                      },
                      "url": "https://nupat.africa"
                    }
                  ]
                }
              ]
              }
